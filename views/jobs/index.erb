<h3>Job List</h3>
<ul>
  <% @jobs.each do |job| %>
    <li class="<%= job.status %>">
      <span class="job-name">
        <% if job.success? %>
          <i class="fa fa-check"></i>
        <% else %>
          <i class="fa fa-close"></i>
        <% end %>
        <a href="<%= job.job_path %>"><%= job.name %></a>
      </span>
      <ul class="list-icon">
        <li>
          <i class="fa fa-github"></i><span>Repository
          <a href="<%= job.owner_url %>" target="_blank"><%= job.owner_name %></a>&nbsp;/
          <a href="<%= job.repository_url %>" target="_blank"><%= job.repository_name %></a></span>
        </li>
        <li><i class="fa fa-github"></i><span>Branch <%= job.local_branch_name %></span></li>
        <li><i class="fa fa-calendar"></i><span><%= job.created_at %></span></li>
      </ul>
    </li>
  <% end %>
</ul>
